@page
@model CamCook.Pages.Recipes.ListModel
@{
    ViewData["Title"] = "Lista de Recetas";
}

<h2>Lista de Recetas</h2>

<div>
    <h3>Aquí están todas las recetas:</h3>
    <ul>
        @foreach (var recipe in Model.Recetas)
        {
            <li>@recipe.Title</li>
        }
    </ul>
</div>
